{
  "devDependencies": {
    "@cap-js/cds-typer": ">=0.1",
    "@cap-js/cds-types": "^0.16.0",
    "@types/node": "^24.0.0",
    "tsx": "^4",
    "typescript": "^5"
  },
  "dependencies": {
    "@cap-js/hana": "^3.0.1",
    "@cap-js/sqlite": "^3.0.2",
    "cds-swagger-ui-express": "^0.11.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.15.1",
    "reflect-metadata": "^0.2.2"
  },
  "scripts": {
    "start": "cds-serve",
    "build": "",
    "watch": "cds watch",
    "deploy": "cds deploy --to sqlite"
  },
  "imports": {
    "#cds-models/*": "./@cds-models/*/index.js"
  },
  "cds": {
    "requires": {
      "db": {
        "[development]": {
          "kind": "sqlite",
          "impl": "@cap-js/sqlite",
          "credentials": {
            "url": "learning_platform.sqlite"
          }
        }
      }
    }
  }
}
